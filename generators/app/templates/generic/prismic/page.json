{
  "Meta": {
    "uid": {
      "type": "UID",
      "config": {
        "label": "Page ID",
        "placeholder": "ID for the Page. Append at the end -en or -de for the language"
      }
    },
    "url_section": {
      "type": "Text",
      "config": {
        "label": "URL section",
        "placeholder": "The section the page is in. Not required if the page doesn't belong to a section."
      }
    },
    "meta_tags": {
      "type": "StructuredText",
      "config": {
        "multi": "embed",
        "label": "Meta Tags",
        "placeholder": "Page's meta tags"
      }
    },
    "meta_script": {
      "type": "StructuredText",
      "config": {
        "multi": "embed",
        "label": "Meta Script",
        "placeholder": "This will be added to a script tag on the HEAD"
      }
    },
    "meta_title": {
      "type": "StructuredText",
      "config": {
        "single": "heading1",
        "label": "Meta Title",
        "placeholder": "Title of the page"
      }
    },
    "schema_description": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph",
        "label": "Schema Description",
        "placeholder": "Schema Description"
      }
    },
    "meta_open_graph_image": {
      "type": "Image",
      "config": {
        "constraint": {},
        "thumbnails": [],
        "label": "Meta Open Graph Image"
      }
    }
  },
  "Content": {}
}
